steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - build
  - -t
  - us-central1-docker.pkg.dev/$PROJECT_ID/market-analysis/stock-service:$SHORT_SHA
  - .

- name: 'gcr.io/cloud-builders/docker'
  args:
  - push
  - us-central1-docker.pkg.dev/$PROJECT_ID/market-analysis/stock-service:$SHORT_SHA

- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
  - run
  - deploy
  - stock-service
  - --image=us-central1-docker.pkg.dev/$PROJECT_ID/market-analysis/stock-service:$SHORT_SHA
  - --region=us-central1
  - --platform=managed
  - --allow-unauthenticated
  - --service-account=market-analysis-sa@$PROJECT_ID.iam.gserviceaccount.com
  - --set-env-vars=PROJECT_ID=$PROJECT_ID,RAW_DS=raw,ANALYTICS_DS=analytics,PRICES_TABLE=Prices,PRICES_WITH_RETURNS=Prices_with_returns,TICKERS_TABLE=Tickers,INTRADAY_TABLE=IntradayPrices,IND_TABLE=Indicators,TODAY_SNAPSHOT_TABLE=TodaySnapshot,AGG_PRICES_TABLE=AggPrices,AGG_RETURNS_TABLE=AggReturns

images:
- us-central1-docker.pkg.dev/$PROJECT_ID/market-analysis/stock-service:$SHORT_SHA

options:
  logging: CLOUD_LOGGING_ONLY
