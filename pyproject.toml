[project]
name = "market_analysis"
version = "0.1.0"
description = "Market analysis platform (Python + Docker)"
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["src"]
